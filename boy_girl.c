#include <stdio.h>

int main ()
{
    char str[101];
    int i;
    scanf("%s", str);
    if( str[i] % 2 == 0){       // số ký tự là chẵn thì là con gái
        printf("CHAT WITH HER!");
    }else
    {
        printf("IGNORE HIM!");
    }

    return 0;
}